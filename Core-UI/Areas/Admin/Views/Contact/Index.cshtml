@using EntityLayer.Concrete
@model List<Contact>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminDashboard.cshtml";
}

<div class="wrapper wrapper-content">
    <div class="row">

        <div class="col-lg-12 animated fadeInRight">
            <div class="mail-box-header">


                <h2>
                   İletişim Formundan Gelen Mesajlar
                </h2>

            </div>
            <div class="mail-box">

                <table class="table table-hover table-mail">
                    <tbody>
                        @foreach (var item in Model)
                       {
                            <tr class="unread">

                            <td class="mail-ontact"><a href="/Admin/Contact/Inbox/@item.ContactId">@item.ContactUserName</a></td>
                            <td class="mail-subject"><a href="/Admin/Contact/Inbox/@item.ContactId">@item.ContactSubject</a></td>
                            <td class="text-right mail-date"><i class="fa fa-clock-o"></i>@item.ContactDate</td>
                        </tr>
                       }
                       

                    </tbody>
                </table>


            </div>
        </div>
    </div>
</div>